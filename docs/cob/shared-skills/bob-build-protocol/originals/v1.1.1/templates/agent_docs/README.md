# agent_docs · {{PROGRAM_TITLE}}

Detail loaded on demand, never at start. One file per topic. Each line here is the whole index.

- `security.md`: what must never leak, which paths are protected, how secrets reach the build (they do not; a proxy does).
- `performance.md`: the budget for page weight, response time, run time, credits; how it is measured.
- `{{mechanism}}.md`: the playbook steps for this program's mechanism, with the "why this step exists" line on each so a stale step can be retired.
